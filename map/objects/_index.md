# Public JAS object index

All entries are `status: stub` until a human-reviewed citation pass.

- `catalog-and-taxonomy` — `catalog/manifest.json` and catalog/taxonomy
  surfaces; `universe: live`; does not own private package bytes.
- `portable-contracts` — `library/`, `skills/`, `workflows/`, and reusable
  templates; `universe: live`; does not own host-specific installation.
- `host-adapters` — `adapters/`; `universe: live`; does not redefine portable
  contract truth.
- `manifest-runtime-and-verification` — `bin/`, `src/`, `schemas/`,
  `scripts/`, and tests; `universe: live`; does not make runtime output an
  independent catalog authority.
- `private-and-installed-projections` — referenced boundary only;
  `universe: unknown`; does not belong in the public source tree.

