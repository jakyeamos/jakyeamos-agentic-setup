# Codex adapter edge

This is sanitized, staged guidance for Codex. It is not a live `AGENTS.md`,
host lifecycle file, session store, or registration recipe.

- Route to the nearest repository context index before loading a payload.
- Map `context-budget-governor` to a reviewed compaction/checkpoint lifecycle.
- Keep the 150,000-token value as a policy default, not a promise about a
  particular model's behavior.
- Stage `safe-tool-guards` at a documented host edge only after review.
- Keep repository instructions, live truth, and handoff artifacts in their
  declared locations.

This adapter contains no host configuration, credentials, session data, or
registration command.
