interface:
  display_name: "Project Compass"
  short_description: "Reconcile product truth and real progress"
  default_prompt: "Use $project-compass to reorient this project, choose or validate scoped subsystem compasses, and run an intent-first onboarding or realignment quiz when purpose is ambiguous while preserving established conversation decisions and reconciling them with MVP, implementation, and verified project truth."
