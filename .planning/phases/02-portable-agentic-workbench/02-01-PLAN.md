---
phase: "02-portable-agentic-workbench"
plan: "01"
type: execute
status: complete
wave: 1
depends_on: []
files_modified:
  - "catalog"
  - "scripts"
  - "workflows"
  - "adapters"
  - "docs"
  - "tests"
  - "README.md"
  - "AGENTS.md"
  - ".tracker/PROJECT_TRUTH.md"
autonomous: true
requirements:
  - "WB-CATALOG"
  - "WB-INSTALLER"
  - "WB-PUBLIC-SAFETY"
  - "WB-NARRATIVE"
user_setup: []
---

<objective>
Expand the public two-skill package into a dependency-free Portable Agentic
Workbench with a machine-readable catalog, safe installer, vendor-neutral
workflows, staged adapters, evidence case studies, and clean-room proof.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish catalog and public boundary</name>
  <files>catalog/manifest.json, catalog/index.md, AGENTS.md, docs/mining.md</files>
  <action>Define asset classes, provenance, evidence, targets, dependencies, and installation mode. Keep private and external runtime material out of the manifest.</action>
  <verify>python3 scripts/validate_catalog.py</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic CLI and safety gates</name>
  <files>scripts/workbench.py, scripts/catalog_validation.py, scripts/public_safety_check.py, tests</files>
  <action>Implement list, search, show, validate, and dry-run-by-default install with explicit roots, no overwrites, dependency reporting, and staged manual adapters.</action>
  <verify>python3 -m unittest discover -s tests -p 'test_*.py'</verify>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Package workflows, adapters, evidence, and truth</name>
  <files>workflows, adapters, fixtures, docs/case-studies, README.md, SECURITY.md, ATTRIBUTION.md, .tracker/PROJECT_TRUTH.md</files>
  <action>Publish portable workflow contracts and honest case studies, preserve both existing skills, and update live truth without changing the separate QR phase.</action>
  <verify>python3 scripts/workbench.py validate &amp;&amp; git diff --check</verify>
</task>

</tasks>

<acceptance>

- Existing skill validation remains green.
- Catalog validation catches duplicate IDs, missing files, unsupported targets,
  broken links, and invalid provenance.
- Public-safety validation catches private paths, credential values, environment
  files, raw authorization headers, and private artifacts.
- CLI JSON output is deterministic and apply mode works in a disposable target
  without overwriting or touching an implicit home configuration.
- A clean-room checkout needs only Python's standard library.
- Pre-CR runs against a non-empty feature diff; the clean-tree no-coverage
  behavior remains documented as baseline tool behavior.

</acceptance>
